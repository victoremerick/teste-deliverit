spring:
  datasource:
    url: jdbc:mysql://localhost:3306/deliverit
    username: root
    password: senharoot
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none

  flyway:
    locations: classpath:db/migration